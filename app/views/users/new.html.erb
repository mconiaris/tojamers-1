<h1>New user</h1>

<%= form_for @story do |f| %>
<!-- TODO: User should be dynamically included -->
User (should be dynamically entered in):
<%= f.text_field :user %>
</br>
Enter URL
<%= f.text_field :url %>
</br>
Enter Description
<%= f.text_field :description %>
</br>
<%= f.submit "create story!" %>
<% end %>
